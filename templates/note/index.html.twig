{% extends 'base.html.twig' %}

{% block title %}Notes!{% endblock %}

{% block body %}

<div class="container">
       <table class ="table table-light table-striped">
            <thead>
            
                <tr>
                    <th>id</th>
                    <th>title</th>
                    
                    
                </tr>
            <thead>
            <tbody>
                {% for note in notes %}
             
                <tr>
                   <td>{{ note.id }}</td>
                   <td>
                   <a href= "{{ path('notes.show', {id:note.id})}}"</a>
                   {{ note.title }}</td>
                   
                </tr>
             
                {% endfor %}

            </tbody>
        <table>
      
    
{% endblock %}
